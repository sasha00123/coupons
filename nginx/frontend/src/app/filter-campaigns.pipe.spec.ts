import {FilterCampaignsPipe} from './filter-campaigns.pipe';

describe('FilterCampaignsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCampaignsPipe();
    expect(pipe).toBeTruthy();
  });
});
