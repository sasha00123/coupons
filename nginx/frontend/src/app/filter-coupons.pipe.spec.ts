import { FilterCouponsPipe } from './filter-coupons.pipe';

describe('FilterCouponsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCouponsPipe();
    expect(pipe).toBeTruthy();
  });
});
